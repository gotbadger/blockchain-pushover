should   = require('chai').should()
expect   = require('chai').expect

describe 'Processor', ->
	
    beforeEach (done) ->
        done()

  	afterEach (done) ->
    	done()
    
    describe 'constructor', ->
        it 'Test Should Run', (done) ->
            console.log("ran test")
            done()
        
        # it 'fail', (done) ->
        #     done("fail me")

	